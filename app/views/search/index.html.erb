<% content_for :title, t('navigation.search') %>
<div class="container mx-auto px-4 py-8">
  <div class="max-w-4xl mx-auto">
    <h1 class="text-3xl font-bold  mb-8 text-center">
      <%= t('navigation.search') %>
    </h1>
    <div class="rounded-lg shadow-md p-6 mb-8 border border-background-contrast">
      <%= form_with url: search_path, method: :get, local: true,
          class: "flex gap-3", data: { controller: "search-form" } do |form| %>
        <div class="flex-1 w-full">
          <%= form.search_field :q, value: @query,
            placeholder: t('forms.search_placeholder', type: t('site.name')),
            class: "search-input",
            data: {
              action: "input->search-form#search",
              "search-form-target": "input"
            } %>
        </div>
        <%= form.submit t('forms.search'), class: "btn btn-primary px-6 py-3 rounded-lg w-fit" %>
      <% end %>
    </div>
    <% if @query.present? %>
      <% if @query.length < 2 %>
        <div class="bg-warning border border-warning rounded-lg p-4 mb-6 text-warning-foreground">
          <p class="text-warning-foreground">
            <%= t('.search_too_short') %>
          </p>
        </div>
      <% elsif @results.present? && @total_results > 0 %>
        <div class="mb-6">
          <p class=" text-center">
            <%= t('.results_found', count: @total_results) %>
            "<span class="font-semibold "><%= h(@query) %></span>"</p>
        </div>
        <div class="space-y-8">
          <% @results.each do |model_name, results| %>
            <% if results.present? %>
              <%= render "search_section",
                  model_name: model_name,
                  results: results %>
            <% end %>
          <% end %>
        </div>
      <% else %>
        <div class="-subtle border border-background-contrast rounded-lg p-8 text-center">
          <div class="mb-4">
            <%= icon "magnifying-glass", class: "size-12  mx-auto" %>
          </div>
          <h3 class="text-lg font-medium  mb-2">
            <%= t('.no_results_title') %>
          </h3>
          <p class="">
            <%= t('.no_results_description') %>
          </p>
        </div>
      <% end %>
    <% else %>
      <div class="-subtle border border-background-contrast rounded-lg p-8 text-center">
        <div class="mb-4">
          <%= icon "magnifying-glass", class: "size-12  mx-auto" %>
        </div>
        <h3 class="text-lg font-medium  mb-2">
          <%= t('.prompt_title') %>
        </h3>
        <p class="">
          <%= t('.prompt_description') %>
        </p>
      </div>
    <% end %>
  </div>
</div>