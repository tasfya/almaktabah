<div class="min-h-screen py-8">
  <div class="container mx-auto px-4">
    <div class="grid grid-cols-1 gap-8">
      <div class="lg:col-span-2">
        <article class="border border-background-contrast rounded-lg shadow-md p-8">
          <header class="mb-8">
            <% if @lecture.video? %>
              <section class="mb-8">
                <% if @lecture.video? %>
                  <video controls class="w-full rounded">
                    <source src="<%= url_for(@lecture.video) %>" type="<%= @lecture.video.content_type %>">
                    <%= t('common.browser_no_support_video') %>
                  </video>
                <% end %>
              </section>
            <% elsif @lecture.youtube_url.present? %>
              <section class="mb-8">
                <div class="aspect-video">
                  <iframe src="<%= youtube_embed_url(@lecture.youtube_url) %>"
                          frameborder="0"
                          allowfullscreen
                          class="w-full h-full rounded"></iframe>
                </div>
              </section>
            <% end %>
            <h1 class="text-3xl font-bold mb-4">
              <%= @lecture.title %>
            </h1>
            <div class="flex flex-wrap items-center gap-4 text-sm mb-6">
              <% if @lecture.category.present? %>
                <span class="badge badge-primary">
                  <%= @lecture.category %>
                </span>
              <% end %>
              <% if false && @lecture.published_at %>
                <time class="text-foreground"><%= @lecture.published_at.strftime("%Y/%m/%d") %></time>
              <% end %>
            </div>
          </header>
          <% if @lecture.optimized_audio? || @lecture.video? %>
            <section class="mb-8">
              <div class="rounded-lg p-6">
                <h3 class="text-lg font-semibold mb-4">
                  <%= t(".download_options")%>
                </h3>
                <div class="flex flex-wrap gap-4">
                  <% if @lecture.optimized_audio? %>
                    <%= link_to rails_blob_url(@lecture.optimized_audio, disposition: "attachment"), download: true, target: "_blank", class: "btn btn-primary w-fit" do %>
                      <%= icon "arrow-down-tray", class: "size-5" %>
                      <%= t("buttons.download_audio")%>
                    <% end %>
                    <%= play_button(resource: @lecture) %>
                  <% end %>
                  <% if @lecture.video? %>
                    <%= link_to rails_blob_url(@lecture.video, disposition: "attachment"), download: true, target: "_blank", class: "btn btn-secondary w-fit" do %>
                      <%= icon "arrow-down-tray", class: "size-5" %>
                      <%= t("buttons.download_video")%>
                    <% end %>
                  <% end %>
                </div>
              </div>
            </section>
          <% end %>
        </article>
      </div>
    </div>
  </div>
</div>